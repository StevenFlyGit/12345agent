# 前端工程（预留）

此目录用于后续建设 12345 热线工单辅助智能体的 Web 前端。

当前暂不初始化 React、TypeScript 或 Vite，也不创建 `package.json`。完成前端基础环境学习后，再在此目录内创建前端工程；不要把前端依赖安装到仓库根目录或 `backend/`。

规划中的界面流程为：录音或文本输入 → 转写与要素确认 → 工单编辑 → 分类与承办单位审核 → 处理结果录入 → 回复与回访话术确认。
